services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-wazzup_contacts}
      POSTGRES_USER: ${POSTGRES_USER:-wazzup_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wazzup_password}
    volumes:
      - wazzup_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-wazzup_contacts}
      DB_USERNAME: ${POSTGRES_USER:-wazzup_user}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-wazzup_password}
      SERVER_PORT: 8080
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    ports:
      - "${APP_PORT:-8080}:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  wazzup_postgres_data:
